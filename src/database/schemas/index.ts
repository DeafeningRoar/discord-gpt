import conversation from './conversation';
import speakQueue from './speak-queue';

enum models {
  conversation = 'conversation',
  speakQueue = 'speakQueue',
};

export { models, conversation, speakQueue };
